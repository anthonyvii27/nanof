import { ReactElement } from "react";

import { Root, Avatar } from "./styles";

const UserSession = (): ReactElement => (
    <Root>
        <Avatar>AV</Avatar>
    </Root>
);

export { UserSession };
